---
import { socials } from "@data/general";
import { hours } from "@data/general";
import { contacts } from "@data/general";
import { Icon } from "astro-icon";

const date = new Date();
---

<section class="bg-primary px-edge py-10">
  <footer>
    <div
      class="mx-auto flex max-w-screen-2xl flex-col items-center justify-between gap-10 text-center lg:flex-row lg:items-start lg:text-left"
    >
      <div class="mx-auto max-w-sm lg:mx-0">
        <p class="text-lg font-bold">D&D Family Construction</p>
        <p class="mt-2 text-lg">
          Lorem ipsum dolor sit amet consectetur adipisicing elit. Earum non ab
          exercitationem veritatis, labore nobis ipsa officiis magnam sequi,
          iusto asperiores.
        </p>
        <div class="mt-6 flex justify-center space-x-2 lg:justify-start">
          {
            socials.map((social) => (
              <a href={social.link} target="_blank" aria-label={social.name}>
                <Icon name={social.icon} size={30} color="black" />
              </a>
            ))
          }
        </div>
      </div>

      <div>
        <p class="text-lg font-bold">Our Hours</p>
        <ul class="mt-2">
          {
            hours.map((hour) => (
              <li>
                <p>
                  {hour.day}: {hour.open}-{hour.close}
                </p>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <p class="text-lg font-bold">Contact Us</p>
        <ul class="mt-2 space-y-2">
          {
            contacts.map((contact) => (
              <li class="flex justify-center space-x-2 lg:justify-start">
                <Icon name={contact.icon} size={30} aria-label={contact.name} />
                <p>{contact.text}</p>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </footer>
  <div class="pt-4">
    <p class="text-center text-xl">
      Copyright © {date.getFullYear()} D&D Family Construction | All Rights
      Reserved | Powered By <a
        target="_blank"
        href="https://www.simply-sprout.com">Simply Sprout</a
      >
    </p>
  </div>
</section>
